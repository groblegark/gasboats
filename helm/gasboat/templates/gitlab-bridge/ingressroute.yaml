{{- if and .Values.gitlabBridge.enabled .Values.gitlabBridge.ingress.enabled }}
{{- $fullname := printf "%s-gitlab-bridge" (include "gasboat.fullname" .) -}}
{{- $host := .Values.gitlabBridge.ingress.host -}}
{{- $port := .Values.gitlabBridge.service.port | default 8092 -}}
{{- $pathPrefix := .Values.gitlabBridge.ingress.pathPrefix | default "" -}}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ $fullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gasboat.labels" . | nindent 4 }}
    app.kubernetes.io/component: gitlab-bridge
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`{{ $host }}`){{ if $pathPrefix }} && PathPrefix(`{{ $pathPrefix }}`){{ end }}
      kind: Rule
      priority: 100
      services:
        - name: {{ $fullname }}
          port: {{ $port }}
      middlewares:
        {{- if $pathPrefix }}
        - name: {{ $fullname }}-stripprefix
        {{- end }}
        {{- if .Values.gitlabBridge.ingress.ipWhitelist.enabled }}
        - name: {{ $fullname }}-ipwhitelist
        {{- end }}
  {{- if .Values.tls.enabled }}
  tls:
    secretName: {{ include "gasboat.fullname" . }}-wildcard-tls
  {{- end }}
{{- if $pathPrefix }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $fullname }}-stripprefix
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gasboat.labels" . | nindent 4 }}
    app.kubernetes.io/component: gitlab-bridge
spec:
  stripPrefix:
    prefixes:
      - {{ $pathPrefix }}
{{- end }}
{{- if .Values.gitlabBridge.ingress.ipWhitelist.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $fullname }}-ipwhitelist
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gasboat.labels" . | nindent 4 }}
    app.kubernetes.io/component: gitlab-bridge
spec:
  ipWhiteList:
    sourceRange:
      {{- toYaml .Values.gitlabBridge.ingress.ipWhitelist.sourceRange | nindent 6 }}
{{- end }}
{{- end }}
