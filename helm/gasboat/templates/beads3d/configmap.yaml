{{- if .Values.beads3d.enabled }}
{{- $daemonURL := include "gasboat.beads3d.daemonURL" . -}}
{{- $hasDaemonAuth := or .Values.beads3d.daemonToken .Values.beads3d.daemonTokenSecret .Values.beads3d.externalSecret.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gasboat.beads3d.fullname" . }}-nginx
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gasboat.beads3d.labels" . | nindent 4 }}
data:
  default.conf: |
    server {
        listen 80;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        # Patch cached JS bundles: force rest mode (kbeads doesn't speak Connect-RPC on HTTP port)
        sub_filter_once off;
        sub_filter_types application/javascript;
        sub_filter 'mode||"rpc"' 'mode||"rest"';

        # Static assets with cache headers
        location /assets/ {
            expires 7d;
            add_header Cache-Control "public, immutable";
        }

        # SPA fallback
        location / {
            try_files $uri $uri/ /index.html;
            expires 5m;
        }

        # Reverse-proxy API (strips /api prefix) — REST paths
        location /api/ {
            {{- if $hasDaemonAuth }}
            include /etc/nginx/snippets/daemon-auth.conf;
            {{- end }}
            proxy_pass {{ $daemonURL }}/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 30s;
        }

        # SSE events stream (kbeads REST mode)
        location /api/v1/events/stream {
            {{- if $hasDaemonAuth }}
            include /etc/nginx/snippets/daemon-auth.conf;
            {{- end }}
            proxy_pass {{ $daemonURL }}/v1/events/stream;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 3600s;
        }

        # SSE events stream (legacy rpc mode — redirect to REST)
        location /api/events {
            {{- if $hasDaemonAuth }}
            include /etc/nginx/snippets/daemon-auth.conf;
            {{- end }}
            proxy_pass {{ $daemonURL }}/v1/events/stream;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 3600s;
        }

        # SSE bus events stream (kbeads REST path)
        location /api/v1/bus/ {
            {{- if $hasDaemonAuth }}
            include /etc/nginx/snippets/daemon-auth.conf;
            {{- end }}
            proxy_pass {{ $daemonURL }}/v1/bus/;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 3600s;
        }

        # SSE bus events stream (legacy path — rewrite to v1)
        location /api/bus/ {
            {{- if $hasDaemonAuth }}
            include /etc/nginx/snippets/daemon-auth.conf;
            {{- end }}
            proxy_pass {{ $daemonURL }}/v1/bus/;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 3600s;
        }
    }
{{- end }}
