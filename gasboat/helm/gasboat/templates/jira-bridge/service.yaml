{{- if .Values.jiraBridge.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gasboat.fullname" . }}-jira-bridge
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gasboat.labels" . | nindent 4 }}
    app.kubernetes.io/component: jira-bridge
spec:
  type: {{ .Values.jiraBridge.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.jiraBridge.service.port | default 8091 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gasboat.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: jira-bridge
{{- end }}
