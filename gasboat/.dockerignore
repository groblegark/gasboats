# Only send what the agent Dockerfile needs to the build context.
# The gb-builder stage needs controller/ and the slim stage needs images/agent/.
.git
.rwx
*.md
*.yml
*.yaml
!controller/**
!images/agent/**
