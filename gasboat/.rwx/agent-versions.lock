# DEPRECATED: All versions are now defined in gasboat/images/agent/install/versions.env.
# That file is the single source of truth for both Dockerfile and RWX CI.
#
# Per-task lock files (.rwx/agent-*.lock) still exist for cache busting only.
# Bump cache-epoch in each to force a rebuild.
