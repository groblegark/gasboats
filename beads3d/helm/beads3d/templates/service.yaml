apiVersion: v1
kind: Service
metadata:
  name: {{ include "beads3d.fullname" . }}
  labels:
    {{- include "beads3d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beads3d.selectorLabels" . | nindent 4 }}
